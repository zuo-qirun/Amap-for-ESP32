{
  "name": "amap-ota-sync",
  "version": "0.1.0",
  "private": true,
  "description": "Hourly OTA artifact sync for AMap ESP32 releases.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "sync": "node sync-build.mjs",
    "sync:force": "node sync-build.mjs --strict-missing-channel"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "unzipper": "^0.12.3"
  }
}
